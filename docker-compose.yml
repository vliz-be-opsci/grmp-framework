services:
  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: testorchgrmp
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./config:/config:ro
      - ./reports:/reports:rw
    environment:
      - CONFIG_DIR=/config
      - REPORTS_HOST_PATH=${REPORTS_HOST_PATH}
      - SECRET_MY_API_KEY=supersecretkey
    working_dir: /app
    networks:
      - test-network

networks:
  test-network:
    driver: bridge
